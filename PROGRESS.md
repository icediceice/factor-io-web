# Project: factor-io-web

> Initialized: 2026-03-03 15:33
> Last updated: 2026-08-28 06:10 (AI inference TCO calculator shipped — engine, snapshot, client, fixtures F1–F10)

## Current Focus

The AI inference TCO calculator (SPEC v0.1) is implemented and locally verified end to end: decimal-exact engine, frozen-feed contract tests, live pricing snapshot, zero-build client page, and browser-verified S1–S4 flows. Remaining work is operational: snapshot refresh automation, evidence ingestion for Lane C measured rates, feed TTL calibration.

## Task Queue

Upcoming work in priority order:

- [ ] Snapshot refresh automation (scheduled rebuild + staleness banner is already client-side; needs a cron/keepalive publisher per SPEC §5.6 — commit timestamps are never freshness evidence)
- [ ] Evidence ingestion: replace the empty evidence store with cited measured tok/s rows (all-dimensions match rule) and re-verify Lane C presets against vendor pages
- [ ] Calibrate feed TTLs and N_CONSECUTIVE_RETIRE against real feed update cadence (SPEC §5.1 defaults are [ASSUMED])
- [ ] Define tech stack and scaffold project source code

## Tier Overrides

*(Populated by escalation events. Survives compaction — do not remove.)*

## Work Log

### 2026-08-28

#### 07:05 — peer verify resolutions G1–G9 implemented; snapshot re-keyed and re-shipped
- **What:** The size-L peer verification returned 9 blocking findings; all were validated against source, reconciled where the peer's proposed fix diverged from the architecture, and implemented once. G1: OpenRouter override schedules were silently dropped in production — conditions were parsed through parseJSONExact (numeric literals arrive as text) but accepted only when `Number.isFinite` (numbers), so every entry skipped; and `utcMinutesDay` derived the weekday from `toISOString().slice(0,3)` — the year prefix — so day-window matching could never hit. Fixed with a strict exact-integer coercion (`conditionInt`) keeping the single-parser invariant (reconciled from the peer's JSON.parse suggestion: one parse path for prices and conditions is the load-bearing rule) and a `getUTCDay` sun–sat map accepting the feed's full day names. G2: offers were keyed by shared canonical_slug so `:batch`/`:free` variants overwrote their paid sibling (50%-mispriced); now keyed by unique model id with canonical_slug as lineage, aliases skipped with a log, duplicate ids thrown. G3: offer-state transitions recorded labels but dropped payloads — omissions/errors now carry the last-good payload into the catalog (suspect quotes flag `price_stale_risk`; retired leaves the selectable index). G4 (DIVERGED): fixed_split prorated Lane A's precomputed total by the pinned share — 50% × $10,000 = $5,000 fractionally discounts a fixed asset, against the plan's charged-exactly-once promise; replaced with per-allocation lane evaluation (fixture 5400 → 10400). G5: Lane C treated tokens/second as tokens/hour — every cost and breakeven 3600× too high; ×3600 added at all three sites (fixture hours 800000/17 → 2000/153, breakeven 88.2353 → 0.0245). G6: the no-bucket route ignored the monthly budget (overflow silently zero) and the UI passed `time_buckets: null` despite promising "spread over month"; engine caps local at budget, UI now supplies an explicit 730 h whole-month bucket. G7: non-terminating overflow/failover rationals hit `Dec.from(null)` crashes or were dropped from totals; lane totals are now Money (Rat accumulators) serialized through the exported `ratStr` — canonical decimal when terminating, "n/d" when not (F7 strings byte-identical). G8: S3 never rendered Lane A or the recommendation; added the Lane A row (per-1m now emitted) and a prominent "Recommended — policy" card so the F7 optimum is a visible number. G9: the sensitivity grid was fake arithmetic (`base × dm × pm` in floats); each cell is now a fresh `runComparison` (demand+requests scale together; the price axis re-quotes an exactly scaled tariff clone). Snapshot rebuilt through three honest refreshes so the id migration walked the state machine (old keys suspect → suspect → retired; variants split under id keys: 2,871 selectable models, 2963 active / 205 retired / 0 suspect; budgets hold at 40.3 KB + 122 KB gz); stale content-addressed catalogs removed.
- **Files:** tco-calculator/pricing.js, tco-calculator/calculator.js, scripts/build-snapshot.mjs, tco-calculator/app.js, tests/{calculator-fixtures,snapshot,pricing-contract}.test.mjs, tco-calculator/data/{manifest.json, catalog-9ee58deaa11277c7.json (new), catalog-2077f2a16bf78b00.json (deleted)}
- **Verified:** 60/60 tests green (`node --test 'tco-calculator/tests/*.test.mjs'` — 52 prior + 8 new acceptance tests for G1 production-parse path (raw envelope through parseJSONExact at weekday/wrap boundary instants), G2 duplicate-throw/alias-skip/batch-split, G3 payload carry, G4 per-allocation fixed charge, G5 dimensional check, G6 budget cap, G7 exact "9000017/900" non-terminating overflow). Browser pass at ?v=12 on 127.0.0.1:8787, F7 workload: recommendation card renders **$10,000.00** with Lane B $220.00 (exact, azure/eu/gpt-4o), Lane A $10,000.00 (125.00/1M), Lane C $156.01 (1.95/1M — was $561,632 before the dimensional fix), advisory $10,066.00 dominated delta $66.00 at real prices, breakeven 3,636,363,636 tokens/mo exact, Lane C breakeven util 0.4964, zero console errors; sensitivity grid shows the honest invariance (flat $10,000 while demand fits capacity; exact linear overflow response above it: $10,044/$10,055/$10,068.75 at ×1.5).
- **Next:** live-origin re-check after push; reconcile size-L peer verify and close the plan; evidence ingestion + refresh automation remain queued
- **Known issues:** tco-calculator.html still requires HTTP serving; feed coverage unchanged (LiteLLM + OpenRouter); a handful of OpenRouter override entries legitimately skip as `unrecognized_condition_field` (vendor condition shapes outside the v0.1 grammar — logged per entry, visible in provenance); retired offers accumulate in catalog provenance (bounded; GC is a future builder concern); the offer-id migration is shipped once — external bookmarks to old openrouter:* ids are stale by design

#### 06:10 — AI inference TCO calculator shipped (engine + snapshot + client, fixtures F1–F10)
- **What:** Implemented SPEC v0.1 end to end. Engine (4 modules, zero dependencies): `exact.js` — Decimal (BigInt coeff+scale) for products, gcd-reduced Rational for all quotients, half-up `formatHalfUp` as the only rounding API, `parseJSONExact` so feed number literals never detour through IEEE-754; `pricing.js` — two separate feed grammars compiled to one offer IR (LiteLLM suffix/tiered grammar; OpenRouter `pricing.overrides[]` structured entries with strictly-greater thresholds, wrap-aware UTC windows, later-entries-win per key, verbatim schedule preservation), quote semantics with request-stem evaluation, offer state machine (suspect at 1 miss, retire at 3; `expiration_date` direct-retires; errored sources freeze prior state), per-source freshness envelope (fresh <60% TTL; cron/commit timestamps are never evidence); `calculator.js` — three tariff lanes, both-ceiling demand routing (`min(month remaining, rate×bucket)`; absent temporal data → `capacity_temporal_unknown`), zero-domain guards, local_first derived split with advisory blends flagged dominated (never emitted as optimum), api_first failover, fixed_split, breakeven utilization, TCO curve, sensitivity, all-dimensions evidence gate (empty store → verdict unknown), itemized overlay applied last; `data.js` — schema-major manifest pinning, content-addressed ResourceRef fetch + sha-256 verify, monotonic-generation selection with AbortController race guard, bounded retry → visible gap. Snapshot builder (`scripts/build-snapshot.mjs`, text-fetched + exact-JSON parse) emitted the live snapshot: 2,846 offers, manifest 40.9 KB gz (≤64 KB budget), catalog 109 KB gz (≤256 KB). Client: `tco-calculator.html` + `app.js` (S1 workload → S2 lanes+presets → S3 results with per-number provenance popovers, SVG TCO curve, sensitivity grid, quote export) + `scripts/serve.mjs`. Evidence store ships **empty by mandate** (SPEC 6.5); `lane-c-presets.json` marks all three hardware presets **assumed** with owner + re-verify date (web sources conflicted: 8×H100 $55.04 vs $98.32/hr — shipped as a labelled range, not a measurement). Discoverability: sitemap + llms.txt entries and an index.html bundler card via the Node parse→replace→re-encode method with per-anchor count assertions and `\u002F` closing-tag escapes.
- **Files:** tco-calculator/{exact,pricing,calculator,data}.js, tco-calculator.html, app.js (all new); tco-calculator/tests/{exact,pricing-fixtures,pricing-contract,calculator-fixtures,snapshot}.test.mjs + tests/samples/ (frozen feeds, new); tco-calculator/data/{manifest,catalog-2077f2a16bf78b00.json,lane-c-presets.json,evidence.json} (new); scripts/{fetch-samples,build-snapshot,serve}.mjs (new); sitemap.xml, llms.txt, index.html, PROGRESS.md
- **Verified:** 52/52 node tests green (`node --test 'tco-calculator/tests/*.test.mjs'`) covering F1–F10 against frozen real feeds; live browser pass on 127.0.0.1:8787 confirmed S1–S4: F7 anchor invariants hold in the UI (derived optimum $10,000 with 80M/100M demand; advisory 70/30 blend $10,198 flagged **dominated**, delta $198 at real gpt-4o prices), Lane B tagged exact with meter resolution incl. honest `no_request_fee`, Lane C per-1M $7,020.41 honestly `assumed`, p95 verdicts unknown with the empty-evidence note, staleness banner from the manifest envelope, zero console errors. Byte-identical determinism fixture asserts SPEC 10.1 serialization. Homepage card + nav render post-patch (template JSON parses, 152 `\u002F` escapes, served bytes == disk).
- **Next:** snapshot refresh automation; evidence ingestion for measured tok/s; feed TTL calibration
- **Known issues:** Lane C hourly rates are assumed from conflicting sources (re-verify dates set in lane-c-presets.json; tok/s ceilings are planning placeholders, **not** benchmarks); throughput verdicts are unknown until the evidence store is populated (by design); tco-calculator.html requires HTTP serving (fetches manifest — no file:// support, same caveat as spec-artifact.html); og-image.png/logo.png still 404 (pre-existing); feed coverage is LiteLLM + OpenRouter only — AWS/Azure/GCP bulk registries are spec'd but unimplemented; default model selection picks the first name-sorted /gpt-4o/ match (azure/eu/gpt-4o-2024-08-06), which may surprise users expecting the OpenAI first-party SKU
