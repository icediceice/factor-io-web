# Add ABOVE the `http_status:404` catch-all in /etc/cloudflared/config.yml,
# exactly like the ai.factor-io.com ingress was added for the proxy.
# Keep a rollback copy first: cp config.yml config.yml.bak.<tag>-pre-quotes
#
#  - hostname: quotes.factor-io.com
#    service: http://localhost:80
#
# Then: sudo systemctl restart cloudflared
# DNS: create the CNAME quotes.factor-io.com -> <tunnel-id>.cfargotunnel.com
# (proxied) if it does not exist yet. Rollback = remove the ingress, restart.