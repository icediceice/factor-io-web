{
  "name": "factor-io-quotes",
  "version": "0.1.0",
  "private": true,
  "description": "Factor I/O quotation system — authenticated web app, Chromium-rendered PDF, agent-drivable REST + CLI. Zero runtime dependencies.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "test": "node --test 'tests/*.test.mjs'",
    "smoke": "node smoke.mjs",
    "start": "node server.mjs"
  }
}